#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream file;

    // Create and open the file
    file.open("student.txt");

    // Check whether the file opened successfully
    if (!file) {
        cout << "Error: Unable to create the file." << endl;
        return 1;
    }

    // Write data into the file
    file << "Student Name: Dharshu" << endl;
    file << "Department: Computer Science" << endl;
    file << "Age: 18" << endl;
    file << "C++ Programming" << endl;

    // Close the file
    file.close();

    cout << "File created and data written successfully." << endl;

    return 0;
}
